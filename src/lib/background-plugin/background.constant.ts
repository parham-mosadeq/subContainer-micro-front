export const COMMANDS = {
    insert: 'insert_background_image',
}

export const BUTTONS = {
    default: 'background_btn',
    context: 'background_ctx'
}

export const MENU_ITEMS = {
    default: 'background'
};

export const CONTEXT_TOOLBAR = {
    default: 'background'
};