export { COMMANDS, EVENTS } from './constants/list-plugin.constants'
import { UI_ELEMENTS } from './constants/list-plugin.constants'

export const BUTTONS = {
    insert: UI_ELEMENTS.buttons.insert.name,
}